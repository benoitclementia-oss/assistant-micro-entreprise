# Dépendances de développement — Assistant Micro-Entreprise
# Installer avec : pip install -r requirements-dev.txt

# Inclure les dépendances de production
-r requirements.txt

# Tests
pytest>=8.0
pytest-cov>=6.0
pytest-asyncio>=0.24

# Qualité de code
ruff>=0.8
mypy>=1.13

# Rapports (formats d'export)
coverage[toml]>=7.6
